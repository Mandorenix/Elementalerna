import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.elementalerna',
  appName: 'Elementalerna',
  webDir: 'dist'
};

export default config;
